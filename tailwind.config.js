const config = {
  theme: {
    extend: {
      colors: {
        primary: "#2563eb",
        secondary: "#1e293b",
        light: "#f1f5f9",
      },
      borderRadius: {
        xl2: "1.25rem",
      },
    },
  },
};

export default config;
